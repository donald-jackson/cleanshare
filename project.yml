name: CleanShare
options:
  bundleIdPrefix: dev.cleanshare
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  generateEmptyDirectories: false
settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete
configs:
  Debug: debug
  Release: release
configFiles:
  Debug: Config/Debug.xcconfig
  Release: Config/Release.xcconfig
packages:
  CleanShareCore:
    path: Packages/CleanShareCore
targets:
  CleanShare:
    type: application
    supportedDestinations: [iOS]
    sources:
      - App
    settings:
      base:
        INFOPLIST_FILE: App/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: App/CleanShare.entitlements
    dependencies:
      - package: CleanShareCore
      - target: CleanShareShareExt
        embed: true
  CleanShareShareExt:
    type: app-extension
    supportedDestinations: [iOS]
    sources:
      - ShareExtension
    settings:
      base:
        INFOPLIST_FILE: ShareExtension/Info.plist
        CODE_SIGN_ENTITLEMENTS: ShareExtension/ShareExtension.entitlements
    dependencies:
      - package: CleanShareCore
  CleanShareTests:
    type: bundle.unit-test
    supportedDestinations: [iOS]
    sources:
      - CleanShareTests
    dependencies:
      - target: CleanShare
  CleanShareUITests:
    type: bundle.ui-testing
    supportedDestinations: [iOS]
    sources:
      - CleanShareUITests
    dependencies:
      - target: CleanShare
  ShareExtensionTests:
    type: bundle.unit-test
    supportedDestinations: [iOS]
    sources:
      - ShareExtensionTests
    dependencies:
      - target: CleanShareShareExt
